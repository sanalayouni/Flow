spring.application.name=contentflow

# MySQL Connection
spring.datasource.url=jdbc:mysql://localhost:3306/contentflow_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=41sana41?
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
#jpa is used to automatically create and update the database schema based on the entity classes. The "update" option will update the existing schema without dropping it, while "create" will drop and recreate the schema each time the application starts.
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect